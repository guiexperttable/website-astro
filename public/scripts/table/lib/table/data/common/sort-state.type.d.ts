/**
 * Represents the sort state of a list or table.
 * It can take one of the following values: "asc", "desc", or "" (empty string).
 *
 * @typedef {("asc" | "desc" | "")} SortState
 */
export type SortState = "asc" | "desc" | "";
